const days = ["Saturday", "Sunday", "Monday", "Tuesday"];
const months = ["April", "March", "May"];
console.log("The length af the arrays is " + days.length + months.length);

if (days.length == months.length) {
    console.log ("They are the same")
} else {
    console.log("Two different sizes")
}